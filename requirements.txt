# Core Framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# PDF Processing
PyMuPDF>=1.23.0

# Language Detection (for cost optimization)
fasttext-wheel>=0.9.2
numpy>=1.24.0,<2.0.0  # FastText requires NumPy 1.x (not 2.x)
requests>=2.31.0  # For downloading FastText model

# Azure Document Intelligence (for Arabic PDFs)
azure-ai-documentintelligence>=1.0.0
azure-core>=1.29.0

# Azure Blob Storage
azure-storage-blob>=12.19.0

# Configuration
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-dotenv>=1.0.0

# Database
psycopg2-binary>=2.9.9
sqlalchemy>=2.0.0
gunicorn>=21.2.0

# HTTP client (used for LLM API calls)
httpx>=0.27.0